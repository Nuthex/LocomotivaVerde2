import { SelezionaDirective } from './seleziona.directive';

describe('SelezionaDirective', () => {
  it('should create an instance', () => {
    const directive = new SelezionaDirective();
    expect(directive).toBeTruthy();
  });
});
